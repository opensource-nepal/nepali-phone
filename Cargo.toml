[package]
name = "nepal-phone"
version = "0.1.0"
edition = "2021"
description = "Parse and validate Nepali phone numbers (mobile and landline)."
license = "MIT"

[dependencies]
regex = "1"
once_cell = "1"